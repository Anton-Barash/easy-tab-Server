node_modules/
uploads/
logs/
.env
.env.production
# web/ — сборка фронта (Flutter web). Деплоится на сервер через SCP
# (deploy-web.yml), а НЕ через git. Поэтому папка игнорируется: git-сброс
# при деплое бэкенда не должен затирать свежий фронт.
web/

# Временные log-файлы и артефакты установки
*.log
install_*.log
install_*.cmd
npm_*.log

# Sample-директория (не отслеживать)
sample/
